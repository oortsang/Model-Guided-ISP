# Mini-runs

These scripts use just 10 samples each and are intended to verify all the code is functioning correctly and all the data is in the correct place, etc.
As with the other config files, these can be run with `generate_pipeline_scripts.py` and `submit_pipeline_scripts.py`.
